var dom = fw.getDocumentDOM();
for (var i=0; i<fw.selection.length; i++){
	fw.selection[i].left = dom.left+dom.width/2-fw.selection[i].width/2 ;
	fw.selection[i].top = dom.top+dom.height/2-fw.selection[i].height/2;
}